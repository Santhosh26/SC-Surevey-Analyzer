# AWS Bedrock API Key Authentication
# Copy this file to .env and fill in your actual API key

# AWS Bedrock API Key (recommended method - simpler than traditional credentials)
# Get your API key from: https://console.aws.amazon.com/bedrock/
# Navigate to: API keys > Long-term API keys > Generate long-term API keys
# IMPORTANT: Use AWS_BEARER_TOKEN_BEDROCK (this is the correct variable name that boto3 expects)
AWS_BEARER_TOKEN_BEDROCK=your_bedrock_api_key_here

# Optional: Specify AWS region (default is us-east-1)
# AWS_REGION=us-east-1

# IMPORTANT: Never commit your .env file to version control!
# The .env file should be listed in .gitignore
